import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    // Определите цвета для темной темы
    background: "#1A202C",
    text: "#FFFFFF",
  },
});

export default theme;
