import { Flex } from '@chakra-ui/react';
import React from 'react';

const ProfilesMenu = () => {
    return (
        <Flex width={'100%'} height={'70px'} bgColor={'#000'}>
            
        </Flex>
    );
};

export default ProfilesMenu;